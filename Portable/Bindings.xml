<Binding name="PortableToggle" header="Portable">
    local f = _G["PortableUIFrame"]
    if f then
        if f:IsVisible() then
            f:Hide()
        else
            f:Show()
        end
    end
</Binding>

